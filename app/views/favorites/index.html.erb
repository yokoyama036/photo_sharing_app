<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>Image</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>
 <body>
    <% @favorites.each do |favorite| %>
      <tr>
        <td><%= favorite.picture.content if favorite.picture.content.present? %></td>
        <div><td><%= image_tag favorite.picture.image.url, size: '100x100' if favorite.picture.image.present? %></td></div>
        <td><%= favorite.picture.user.name if favorite.picture.content.present?%></td>
      </tr>
    <% end %>
  </body>
</table>

<%= link_to 'Back', pictures_path %>